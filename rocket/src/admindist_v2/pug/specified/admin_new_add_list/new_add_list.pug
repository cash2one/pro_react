// Main content
section.content
  .row
    .col-xs-12
      .box.box-primary
        // <div class="box-header">
        // <h3 class="box-title">Ion Slider</h3>
        // </div>
        // /.box-header
        .box-body
          p 请选择日期:
          input#datetimepicker(type='text')
          //button.btn.btn-block.btn-success(type='button', style='width:10%;float:right;', data-toggle='modal', data-target='#myModal') #{modal_title}
        // /.box-body
      // /.box
    // /.col
  // /.row
  .row.content_table
    .box
      .box-body
        table.table.table-bordered.table-striped
          tbody#target_list
            tr#target
              td #{loading}
            script(type='text/x-tmpl-mustache',id='template_list')
                tr
                  th(style='width: 10%') 序号
                  th(style='width: 22.5%') 用户名
                  th(style='width: 22.5%') 角色组
                  th(style='width: 22.5%') 手机号码
                  th(style='width: 22.5%') 创建时间
                | {{#new_add_users}}
                tr(data-id="{{user_id}}")
                  td.num 1
                  td.name {{user_name}}
                  td.role(role_group_name='{{role_group_name}}') {{role_group_title}}
                  td.tel {{telephone}}
                  td.create_at {{created_at}}
                | {{/new_add_users}}


      include ../../common_partials/pagination.pug